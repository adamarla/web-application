
-# Form implementation for simultaneous editing of yardsticks. 
-# Same structure and philosophy as for syllabi/edit
-# 
-#  #edit-yardsticks-megaform 
-#    <div class="label large left">....</div>  
-#    <div marker=" .... "> 
-#      <input type='checkbox'/> 
-#      <div class="label small left"> ..... </div> 
-#      <select> 
-#        <option> 
-#      </select>
-# 

- yardsticks = Yardstick.all 

#edit-yardsticks-megaform.mega-form
  = form_tag '/yardstick', :method => :put, :remote => true do |f| 
    = submit_tag "Update"

    #dump
      - yardsticks.each do |yardstick| 
        - prefix = "yardsticks[#{yardstick.id}]"
        %div.space-vertically{ :marker => "#{yardstick.id}" } 
          -#.label.large= "#{yardstick.annotation}"
          %input.full-width{ :type => :text, :name => "#{prefix}[annotation]", :value => "#{yardstick.annotation}" }
          %textarea{ :name => "#{prefix}[description]", :rows => 5}= "#{yardstick.description}"
          .options-box.full-width 
            = nofrills_checkbox :name => "#{prefix}[mcq]", :label => 'mcq ?'
            = nofrills_checkbox :name => "#{prefix}[active]", :label => 'enabled ?'
            = nofrills_checkbox :name => "#{prefix}[subpart]", :label => 'subpart ?'
    #first.column.left 
    #second.column.left
