
%ul.no-bullets
  = main_link :for => :pending, :default => true
  = main_link :for => :schools, :with => {:panels => {:middle => "#new-student", 
                                                      :right => "#new-teacher"}} 

  = main_link :for => :examiners, :with => {:panels => {} }

  = main_link :for => :courses, :with => {:panels => { :middle => '#macro-selection', 
                                                       :right => '#edit-syllabi-form' } }

  = main_link :for => :yardsticks, :with => {:panels => {:middle => '#edit-yardstick'}}

  = main_link :for => :logout, :href => "/logout", :dynamic => false

