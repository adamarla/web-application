!!!5

%html{html_attrs}
  %head 
    %title= "Grades will happen ..."
    = stylesheet_link_tag "application" 
    = javascript_include_tag "application"
    = csrf_meta_tag
  
  %body 
    #viewport
      #public-links
        %ul.no-bullets
          = main_link :for => :videos, :as => 'how-to videos', :href => howtos_path
          = main_link :for => :register, :as => 'get invited to try'
          = main_link :for => :main, :as => 'back to login', :href => root_path
      = yield 
