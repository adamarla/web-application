
#new-student.clear-after-submit
  %legend
    %span.target= "Student"
    %span.operation= "Quick Add"
  = semantic_form_for :student, :remote => true, :url => '#' do |f| 
    .controls
      = submit_tag 'add students'

    = f.inputs do 
      - [*1..5].each do |i|
        = f.input :name, :input_html => {:name => "names[#{i}]", :id => "student_#{i}"}, 
                         :label => "student ##{i}"
