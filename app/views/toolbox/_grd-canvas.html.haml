
= render :partial => 'toolbox/grd-toolbar'
= render :partial => 'toolbox/comment'
-# Can't style the <canvas> using CSS. Hence, setting the width & height here itself
%canvas#grading-canvas{ :width => 600, :height => 800 }
