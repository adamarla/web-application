
#m-register.modal.fade.hide.slim
  %ul.nav.nav-tabs.always-on
    %li
      = simple_link :for => "Teacher", :id => 'tab-register-1', :href => '#pane-register-1'
    %li
      = simple_link :for => "Student", :id => 'tab-register-2', :href => '#pane-register-2'
  .tab-content.pull-left
    #pane-register-1.tab-pane.push-down
      = simple_form_for :register, :url => register_teacher_path, 
      :remote => true, :defaults => { :label => false }, :html => { :id => 'fm-register-teacher' } do |f|
        = f.input :name, :placeholder => "Full Name"
        = f.input :email, :placeholder => "Your e-mail"
        .error.email.hide= "Oops, e-mail address already taken"
        = f.input :password, :placeholder => "Password"
        .error.password.hide= "Password should be atleast 6 characters long"
        = f.input :zip, :placeholder => "Postal Code"
        = f.input :country, :hint => "Select Country", :input_html => { :id => 'rg-country' }
        = f.input :guard, :wrapper_html => { :class => :hide}, :required => false
        %button.btn.btn-large.btn-primary.push-down.pull-left{:type => :submit}= "Register"
    #pane-register-2.tab-pane.push-down
      = simple_form_for :register, :url => register_student_path, 
      :remote => true, :defaults => { :label => false }, :html => { :id => 'fm-register-student' } do |f|
        = f.input :name, :placeholder => "Full Name"
        = f.input :email, :placeholder => "Your e-mail"
        .error.email.hide= "Oops, e-mail address already taken"
        = f.input :password, :placeholder => "Password"
        .error.password.hide= "Password should be atleast 6 characters long"
        = f.input :code, :placeholder => "Group Code", :hint => "Group Code is provided by your teacher"
        .error.sektion.hide= "Code not found! Did you forget the hyphen (-)?"
        %button.btn.btn-large.btn-primary.push-down.pull-left{:type => :submit}= "Register"
        = f.input :guard, :wrapper_html => { :class => :hide}, :required => false
